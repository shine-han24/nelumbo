# 저장소 안에서는 LF로 통일한다. 윈도우에서 작업해도 커밋은 LF가 된다.
* text=auto eol=lf

# 폰트는 바이너리 — 절대 줄바꿈 변환을 하면 안 된다
*.woff2 binary
*.woff  binary
*.ttf   binary
*.otf   binary
*.png   binary
*.jpg   binary
*.ico   binary
